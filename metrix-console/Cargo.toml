[package]
name = "metrix-console"
version = "0.1.0"
edition = "2018"
authors = ["Jon Fast <jnfstdj656@gmail.com>"]

[dependencies]
tui = { version = "0.15", default-features = false, features = ['crossterm'] }